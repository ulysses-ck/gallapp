import React from "react";

const Header = () => {
    return (
        <nav className="navbar navbar-dark bg-gray h1 bold">
            <div className="container-fluid">
                <span className="navbar-brand">GallApp</span>
            </div>
        </nav>
    );
};

export default Header;
